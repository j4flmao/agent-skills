# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
# WCAG 2.1 Accessibility Code

## Matrix and Decision Trees
- Contrast ratio > 4.5:1
- ARIA attributes required.

## Code Examples (TypeScript)
```typescript
export const Button = ({ label, ariaLabel }: Props) => {
    return <button aria-label={ariaLabel}>{label}</button>;
};
```
